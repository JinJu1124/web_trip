extends ../layout

block content
  .container
    h1.main-title Edit Tour
    form(action=`/tours/${tour.id}?_method=PUT`, method="POST")
      input(type="hidden", name="_method", value="POST")
      include _form
      button.btn.btn-outline-primary(type="submit") Update Your Tour
